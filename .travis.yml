virtualenv:
  system_site_packages: true
language: python
python:
  - "3.6"

# install dependencies and sloth
install:
  - pip install numpy pillow python-qt5 qtpy
  - pip install .

# run tests 
script: pytest
